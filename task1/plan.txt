Terraform used the selected providers to generate the following execution plan. Resource actions are indicated with the following symbols:
  + create

Terraform will perform the following actions:

  # google_sql_database.tenant_db will be created
  + resource "google_sql_database" "tenant_db" {
      + charset   = (known after apply)
      + collation = (known after apply)
      + id        = (known after apply)
      + instance  = "main-instance"
      + name      = "acme-corp-db"
      + project   = (known after apply)
      + self_link = (known after apply)
    }

  # google_sql_user.tenant_user will be created
  + resource "google_sql_user" "tenant_user" {
      + host     = (known after apply)
      + id       = (known after apply)
      + instance = "main-instance"
      + name     = "acme-corp-user"
      + password = (sensitive value)
      + project  = (known after apply)
    }

  # random_password.db_password will be created
  + resource "random_password" "db_password" {
      + bcrypt_hash = (sensitive value)
      + id          = (known after apply)
      + length      = 16
      + lower       = true
      + min_lower   = 0
      + min_numeric = 0
      + min_special = 0
      + min_upper   = 0
      + number      = true
      + result      = (sensitive value)
      + special     = true
      + upper       = true
    }

Plan: 3 to add, 0 to change, 0 to destroy.
